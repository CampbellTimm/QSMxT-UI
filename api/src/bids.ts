import { Express } from "express";
import fs from "fs";
import path from "path";

export const setupBidsEndpoints = (app: Express) => {
  // app.get('/bids', getBids)
  // app.get('/bids/tree', getBidsTree)
}